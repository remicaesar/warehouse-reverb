#include "DecayCurve.h"

// Header-only: every function above is a plain free function, all defined inline. Nothing to add
// here -- this translation unit exists only so CMakeLists.txt has a matching .cpp to compile
// (house pattern, see src/dsp/SchroederAllpass.cpp).
