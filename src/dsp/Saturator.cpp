#include "Saturator.h"

// Header-only: every member above is defined inline in the class body. Nothing to add here -- this
// translation unit exists only so CMakeLists.txt has a matching .cpp to compile (house pattern, see
// src/dsp/SchroederAllpass.cpp).
